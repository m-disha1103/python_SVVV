#28 april 2026
#Fraud Transaction Detection 
amount=float(input("Enter the transaction amount: "))
location=input("Enter the transaction location: ")
while(transaction):
    if transaction (3):
        transaction=(int(input("Enter the transaction type (online/offline): ")))
        if amount > 1000 and location != "home" and transaction == 1:
        print("Potential Fraud Detected!")
else:

